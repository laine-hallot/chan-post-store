{
  // Solution-style root: it owns no sources itself, it just points at the
  // workspace packages. Without an empty "files", tsc globs the whole tree
  // and reports TS6305 against the referenced project's own sources.
  "files": [],
  "references": [
    {
      "path": "packages/cli"
    }
  ]
}
